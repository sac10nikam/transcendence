server:
  port: 9101
spring:
  cloud:
    config:
      allowOverride: true
      overrideNone: true
      server:
        git:
          uri: git@github.com:yan-hai/transcendence.git
          clone-on-start: true
          force-pull: true
          delete-untracked-branches: true
          search-paths: config_repo

