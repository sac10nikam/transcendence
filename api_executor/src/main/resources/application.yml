spring.application.name: api-executor
spring:
    cloud:
        stream:
            bindings:
                output:
                    destination: memberReqTopic
                    content-type: application/json
            kafka:
                binder:
                    brokers: localhost
